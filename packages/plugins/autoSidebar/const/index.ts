import path from 'path'

export const DEFAULT_IGNORED_FILES = ['.DS_Store']

export const DEFAULT_IGNORED_DIRS = ['.vuepress', '.vitepress', '.git', '.idea', '.github']

export const VALID_HOME_PAGE_NAME = ['index.md', 'README.md']

export const ROOT_DIR = path.resolve('.')
